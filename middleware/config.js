// Configuration settings
const config = {
    sessionSecret: 'your_session_secret', // Change this to a secure secret
    sessionMaxAge: 3600000, // Session expiration time (1 hour in milliseconds)
  };
  
  module.exports = config;